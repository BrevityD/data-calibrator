# 实验零：测量度规

在进行了部分实验一之后，发现一些思路和设置有问题，积重难返，所以干脆重开一个。

与实验一的主要区别：
1. 全面换用signSGD（也就是一二阶动量参数为0的AdamW），可以保证每一步的迭代模长都是lr。
2. 根据token数决定batch（这样应该可以使得梯度的方差一致）(但目前没有实现，先跑了再说)
3. 加入对（所有数据集的）梯度的均值和方差的考虑
4. 先增加一个理论上限（以较小的学习率跑特别长的训练，直到其loss收敛，作为理论上限的标定）
5. 先抛弃较难评估的logic，先在math和code上跑出来再说